spring:
  cloud:
    config:
      uri: http://zippyziggy.kr:8888
      name: prompt
    cloud:
      kubernetes:
        secrets:
          enabled: true
          paths:
            - /etc/secretspot
          enableApi: false
  profiles:
    active: default
